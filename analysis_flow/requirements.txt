# =============================================================================
# KRA-ORA Medical Analysis System v3.0 - Backend Dependencies
# Python 3.10+ required
# =============================================================================

# Local LLM Inference (GGUF models via llama.cpp)
# ── IMPORTANT: Do NOT install llama-cpp-python via this file. ──
# backends.bat installs it separately with the correct CUDA wheel:
#   pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu118
# If you need to install manually (CPU-only fallback):
#   pip install llama-cpp-python

# Model download (first-time setup only)
huggingface-hub>=0.20.0

# Embedding model for FAISS retrieval
sentence-transformers>=2.2.2

# Vector Database & Supabase
faiss-cpu>=1.7.4

# API Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
python-multipart>=0.0.6

# HTTP Client
requests>=2.31.0

# Data Processing
numpy>=1.24.0

# Utilities
python-dotenv>=1.0.0
tqdm>=4.66.0

# Email Validation
email-validator
